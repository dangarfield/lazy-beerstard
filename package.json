{
  "name": "lazy-beerstard",
  "version": "0.0.0",
  "description": "TUI Maker Fair Idea",
  "author": "Dan Garfield",
  "license": "ISC",
  "devDependencies": {
    "standard": "^10.0.2"
  },
  "scripts": {
    "start": "node lazy-beerstard.js",
    "deploy": "now --public --token $NOW_TOKEN_LAZY_BEERSTARD && now alias --token $NOW_TOKEN_LAZY_BEERSTARD && NOW_TOKEN=${NOW_TOKEN_LAZY_BEERSTARD} now-pipeline-prune && now ls lazy-beerstard --token $NOW_TOKEN_LAZY_BEERSTARD"
  },
  "dependencies": {
    "aws-sdk": "^2.257.1",
    "body-parser": "^1.18.3",
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.3",
    "image-data-uri": "^1.1.1",
    "lodash": "^4.17.10",
    "request": "^2.87.0",
    "string-similarity": "^1.2.0"
  }
}
